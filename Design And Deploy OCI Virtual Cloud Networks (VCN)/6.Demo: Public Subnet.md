### **Introduction to the Demo**

*   Hello and welcome to this new demo. My name is Sergio Castro with Oracle University.
*   And probably you're halfway through your coffee now that we're doing these demos. Let's go ahead and create a public subnet.

### **Step 1: Navigating to the VCN**

*   So I'm going to access my console. Let's go ahead and do that. And as you can see, I'm already logged in.
*   And here, I'm going to go into virtual cloud networks.
*   We previously created a manual VCN and a wizard VCN. And we saw that the wizard VCN comes with two subnets, and the manual VCN does not.
*   So let's go ahead and add a subnet to the manual VCN, the VCN that we created manually.

### **Step 2: Initiating Subnet Creation**

*   And here, it's very easy to create a subnet. You need to click on the Create Subnet button.
*   You will see subnets to the left under Resources. And if you're not there and obviously in this case, I'm en route tables. So I'm not in subnets. So I'm going to go into Subnets and click Create Subnet.

### **Step 3: Configuring the Subnet Details**

*   You're going to give it a name. So I'm going to call this **MyPublicSubnet**. So this will hint that this is a public subnet.
*   You need to select the compartment. Now it's really important to know that it doesn't have to be this compartment. You can have the VCN in one compartment and the subnets in totally different compartments. So that's according to your own design. In this case, we're staying with the same compartment.

#### **Subnet Type: Regional vs. Availability Domain-Specific**

*   There's two configurations of the subnet, either a regional subnet, which is recommended by Oracle Cloud Infrastructure or an availability domain specific subnet.
*   San Jose is a region that only has one availability domain. So you might think what's the difference if I select availability domain specific subnet or a regional subnet, and why is this one recommended?
*   So we have one availability domain for now. If in the future we do add additional availability domains and that might happen, then your subnet is already prepared. And if we add a second availability domain, you can add resources on that second availability domain once it becomes available. So that's the main difference.

#### **Configuring the CIDR Block**

*   The CIDR block is really important that you select one that is within the same VCN CIDR block. If you don't, then you're going to get an error.
*   So I'm going to use this one, the same one that is being proposed to us. The reason why is because I selected the VCN that will be the parent to this subnet.
*   And that's a slight 16. If I change this slightly, you will see an error right there. It's not conforming. So you cannot add a subnet CIDR that doesn't belong to the VCN.
*   So in this case, it's 10.0.0/24, and that's going to be for my public subnet.
*   I cannot add a IPv6 prefix because my VCN, it's not enabled with IPv6. If it was enabled, then I will have this available to me.

#### **Setting the Subnet Access**

*   Now we have the axis. Is this a public or a private subnet? So we name it public subnet. So obviously, we're going to go with a public subnet. This is the axis.

### **Step 4: Finalizing the Configuration**

*   Everything else, I'm going to leave as is. The DNS label is going to be generated for me. If I enable that on the VCN and here I enable it here and the subnet as well. And then this is pre-selected for us.
*   Here, if I don't select DHCP option, it's going to select by default one for me. So it's the same if I select one here or not. In this case, I'm going to select it.
*   Because I do have a default security list for the manual VCN, that's the only option that I have. And remember that this one comes with three rules, one for SSH and two for ICMP.
*   I'm not going to enable VCN logging for this one or subnet logging, so that's all you need. This is creating a subnet, a public subnet.

### **Conclusion**

*   And once this provisions, then you can go ahead and start assigning adding resources. And they will have the option of having a public IP address.
*   So this completes this demo. Thank you very much for watching. Bye.
