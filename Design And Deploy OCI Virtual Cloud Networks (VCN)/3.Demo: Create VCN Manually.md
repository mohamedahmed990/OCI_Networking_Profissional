### **Introduction to the Demo**

*   Hello and welcome. My name is Sergio Castro with Oracle University. And now it's time for the fun part. It's time to get your hands dirty.
*   We're doing a demonstration. We're going to log in into the OCI console and create a Virtual Cloud Network manually.
*   So go ahead and fill your cup with coffee, and let's go ahead and get started.

### **Step 1: Navigating to the VCN Section**

*   I'm going to log in into the OCI. As you can see, I'm already logged in into OCI. And I am in the San Jose region. And we're going to be creating a Virtual Cloud Network.
*   The sure way to get to Virtual Cloud Networks is right here in the top left next to Oracle Cloud. You will see what we call the hamburger icon. Click on it and scroll down all the way to Networking. And you will see Virtual Cloud networks there.
*   It is time for you to select your compartment. As you can see, I already pre-selected the certification compartment. That's the compartment that we're using today.

### **Step 2: Initiating the VCN Creation**

*   You will see two options:
    *   **Create VCN:** This is creating a Virtual Cloud Network manually.
    *   **Start VCN Wizard:** So that's for another lab.
*   Today we're creating the VCN manually. So let's go ahead and do that. And basically, you click on the Create VCN button.

### **Step 3: Configuring the VCN Details**

*   It's going to be asking you to give it a name. So in this case, we're going to call this **ManualVCN**.
*   Make sure that you select the right compartment. We previously selected certification. And that's what you have in front of you, certification.
*   And now we're going to go ahead and give it an IPv4 CIDR block. So as you can see, I don't have any pre-selected. So I have to type it. And I'm going to go with the recommendation, which is **10.0.0.0/16** as you can see it right down below here. And you need to actually click on it for the selection to stay.
*   Then we are going to be leaving out the defaults. The DNS resolution is fine. This is the DNS label.
*   We're not selecting IPv6 prefix in this occasion. But if you want to do that, you can go ahead and click IPv6 right here. Select this checkbox, and it's going to be assigning you one from Oracle Cloud Infrastructure. If you bring your own IPv6, then you can go ahead and add from your own pool of IPv6 prefixes.
*   And that's it. That's all you need to do for creating a Virtual Cloud Network.
*   Now if you are into Terraform, it gives you the option for selecting a stack. So this VCN that you're creating is going to create all the Terraform steps for it. So in this case, we're not doing that.
*   And here, the advanced options before we click Create VCN, let's go ahead and see what we have. And as you can see, if we want to tag our VCN, in this case, we're going to bypass that and just go ahead and create the VCN.

### **Step 4: Post-Creation Overview**

*   And as easy as that, you have your manual VCN.
*   As you can see, we have our manual VCN. But we have no subnets. We have no gateways, no internet gateway. We have no network security groups.
*   We do have security lists. We do have a route table. And we do have a CIDR block prefix because we assigned one. And then we don't have local peering gateway.
*   So all of that, you need to add later on if you wish to do so. Obviously, if you're going to be adding resources to this VCN, you need to have a subnet.

### **Step 5: Exploring Related Resources and Adding an Internet Gateway**

#### **Subnets**
*   So let's go ahead and add a subnet into it. We're just going to show you the process of creating a subnet here. This is how you create a subnet. We're not going to be creating it right now.

#### **Route Table**
*   One thing that I want to show you is the route table. It does come with a route table but is empty. It has no rules.
*   If you want to add a rule, here you can add an additional route table. But if you want to add a route to the default route table, you need to have a gateway or a next hop that you're going to.
*   So if you don't have that, it's not going to allow you to create the route rule itself. Here, we don't have a dynamic routing gateway. We don't have an internet gateway. And we don't have any other gateway or the private IP.
*   So here, if I select internet gateway, I need to specify the internet gateway. And here, it's not available. So I cannot add the route rule. It's indicating that I don't have one.

#### **Creating an Internet Gateway**
*   Let's go ahead and cancel out of here and go ahead and create an internet gateway here. And there we go, create internet gateway. And I'm going to call it **Demo**.
*   And there we go. Now we have an internet gateway.
*   So basically, when you create a manual VCN or without using the wizard, it comes with no related resources. Now the route table was empty.

#### **Security Lists**
*   But what if I see the security list? The security list is populated.
*   And you will see that it's populated with port 22 available for anybody, the open internet. That's what the quad zero means. And it's allowing a stateful rule.
*   A stateful rule basically allows in and out. You don't have to specify an inbound or an outbound rule. In this case, this is an ingress rule, so traffic coming in.
*   If this is a stateful, in this case, it is because you have the stateless marks set to No. So this is stateful means that because you have an English rule, you don't have to specify an outbound egress rule. And it's allowing port 22.
*   And then we have two ICMP rules pre-populated. And these are for troubleshooting. If you want to ping any instance that you might have on a compute instance that you built on this VCN, then you need to specify type 8 for echo.

### **Conclusion**

*   And that's basically the demo for manual VCN. Thank you for watching.
