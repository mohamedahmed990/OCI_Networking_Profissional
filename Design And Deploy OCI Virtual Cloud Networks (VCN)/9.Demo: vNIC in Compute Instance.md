
### **Introduction to the Demo**

*   Welcome to this new demo on Virtual Network Interface Card, the vNIC. So we have gotten our hands dirty. And this time, we're going into the mud. So let's go ahead and get started.
*   For demonstrating the vNIC, we need to create a compute instance, and then attach the vNIC to a Virtual Cloud Network.
*   We're going to be selecting the manual VCN, the one that comes with almost nothing. So let's go ahead and do that.

### **Step 1: Navigating to Compute Instances**

*   I'm going to access into my OCI Console. And as you can see, I'm back into the main dashboard.
*   This time, we're not going to Virtual Cloud Networks. We're going to compute instances.
*   You go into the hamburger icon. You scroll down all the way to compute. And then click Instances.
*   As you can see, I don't have a single instance here. I am still in my San Jose region. I'm still in my certification compartment.

### **Step 2: Creating a Compute Instance and Primary vNIC**

*   Let's go ahead and create an instance here. And we are going to call this **My VM**.
*   Because you need to attach it to a Virtual Cloud Network, it comes with a primary vNIC. And we will be creating a second vNIC.
*   We are in the right compartment. We are in the only availability domain that we have for us. And we are going to be using the default image, the default shape.

#### **Configuring the Primary vNIC**

*   Here's the primary vNIC information. So I'm going to call this **My Primary vNIC**. Because this will be my primary vNIC.
*   What VCN do we need to attach it? We are going to be selecting the **manual VCN**.
*   I'm going to select this in my **public subnet**. Because later on, I do want to have the capability of SSH'ing into it.
*   I do want to automatically assign a private IP address to it, a private IP address. And then I'm going to let Oracle assign the private IP address as well.
*   I don't have IPv6. So I'm going to be remaining without IPv6.
*   I don't have an SSH key in this local machine. So I'm going to download one, the one specifically for this compute instance.
*   Let's go ahead and see the advanced options. We're going to skip all of them. We don't want any advanced option for us, no scripting.
*   All I have to do is go ahead and click **Create**.

### **Step 3: Reviewing the Primary vNIC**

*   Now my virtual machine is in the process of being created. I don't have to wait for it to start working with my vNICs.
*   As you can see, this is my primary vNIC. It's assigning a private IP address, a public IP address.
*   I don't have a network security group. The subnet is the one that I selected, the private subnet.
*   If I scroll down all the way to the resources, you will see that I have a section called **Attached vNICs**. And here, I do have my primary vNIC.
*   If I click here on view the details, I can see what information I have for my primary. These are the metrics. This is the IPv4 addresses. Now I can see that it assigned 185 from my pool. And this is the public IP address that was assigned to it.
*   You can see the skip source and destination check to no. You can change that to yes.

### **Step 4: Creating a Secondary vNIC**

*   If I go into my attached vNICs, you will see that I only have one. But I can go ahead and create a second vNIC.
*   This will be my secondary-- or my second vNIC.
*   I can go ahead and select a different VCN if I want to. I'll stay with manual VCN.
*   I can go ahead and, for advanced settings, work with VLAN. But this is only applicable if I'm using with VMware. In this case, I'm not. So I'm going to go ahead and select my normal subnet. And here, I'm going to stay with my public subnet.
*   In this case, I'm going to manually assign an IP address to it. So I'm going to be assigning 10.0.0.2. So it's the first available address. And that's my private IP address.
*   Because it's in a public subnet, I will assign a public IPv4 address.
*   Here, we haven't select IPv6. If I click on it, it doesn't check the checkbox. So that's all I need to do to create my second vNIC.

### **Step 5: Editing the Secondary vNIC and Enabling IPv6**

*   Let's say that I do want to enable the "skip source/destination check". So if I want to edit. And you can see that the skip source and destination checkbox is unchecked. So I can go ahead and check it.
*   I remember calling it **My Secondary vNIC**. So I can go ahead and edit my vNIC. And here you can see that, now, it did populate the name field with the correct name, skip source and destination. And that's all I need. So now I can skip the source and destination.

#### **Enabling IPv6 on the VCN and Subnet**

*   I didn't get to select an IPv6 address. So let's go ahead and change that.
*   If I go into my VCN, I can go ahead and enable IPv6. So add a CIDR block or IPv6 prefix. In this case, I'm going to be adding IPv6. So all you have to do is click this little checkbox. And this is going to be selected from my pool of IPv6 addresses from Oracle, because I have not brought my own IPv6 address.
*   That's all I need to do. Now, I need to enable the subnet as well. So this is the public subnet. And here you see IPv6 prefix. So I add an IPv6 prefix. And assign one. And I'm going to be use the recommended hexadecimal character, 7E. And there it is. And add an IPv6 prefix to my subnet.

#### **Assigning an IPv6 Address to the Secondary vNIC**

*   Now I can go back into compute, and enable IPv6 on my secondary vNIC.
*   Remember that I need to scroll down all the way to Resources. Select my attached vNIC. And my secondary vNIC is right there.
*   Now you can see that I have IPv4 and IPv6 address. So assign an IPv6 address.
*   Then I'm going to be selecting from the only one that I have. Remember that you can have up to five, of which one is provided by Oracle. And the other four, you have to bring your own IPv6 to OCI. So there it is assigned.

### **Step 6: Adding a Secondary Private IP Address**

*   Now, not only do I have my primary IP address for my secondary vNIC. This is my public IP address as well. And this is my IPv6.
*   And here I can add a secondary private IP address. I'm going to let Oracle select this one for me. So, there it is. The message is gone. And this public IP type.
*   And that's all I need. There we go.
*   So now I have two IP addresses for my secondary vNIC, the primary IP address, IPv4, and the secondary IPv4 address. I don't have one public. I didn't assign one. And let's see if it assigned a secondary one. And I need to assign it manually as well.

### **Conclusion**

*   That completes this demonstration. Thank you very much for watching.
